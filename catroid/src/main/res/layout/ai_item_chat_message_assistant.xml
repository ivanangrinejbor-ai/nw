<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:gravity="top"
    android:paddingStart="4dp"
    android:paddingEnd="24dp"
    android:paddingTop="6dp"
    android:paddingBottom="6dp">

    <TextView
        android:layout_width="30dp"
        android:layout_height="30dp"
        android:layout_marginTop="2dp"
        android:layout_marginEnd="8dp"
        android:background="@drawable/ai_avatar_bg"
        android:gravity="center"
        android:text="@string/ai_agent_avatar_label"
        android:textColor="@color/solid_white"
        android:textSize="11sp"
        android:textStyle="bold" />

    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:orientation="vertical">

        <TextView
            android:id="@+id/message_content"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:background="@drawable/chat_bubble_ai"
            android:lineSpacingExtra="2dp"
            android:textColor="@color/ai_text_primary"
            android:textColorLink="@color/ai_accent"
            android:textSize="15sp" />

        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:layout_marginTop="2dp"
            android:layout_marginStart="4dp">

            <ImageButton
                android:id="@+id/btn_copy"
                android:layout_width="30dp"
                android:layout_height="30dp"
                android:padding="6dp"
                android:scaleType="fitCenter"
                android:src="@drawable/ic_copy"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:contentDescription="@string/ai_agent_copy"
                android:alpha="0.65"
                android:visibility="gone" />

            <ImageButton
                android:id="@+id/btn_regenerate"
                android:layout_width="30dp"
                android:layout_height="30dp"
                android:layout_marginStart="6dp"
                android:padding="6dp"
                android:scaleType="fitCenter"
                android:src="@drawable/ic_regenerate"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:contentDescription="@string/ai_agent_regenerate"
                android:alpha="0.65"
                android:visibility="gone" />

        </LinearLayout>

    </LinearLayout>

</LinearLayout>
