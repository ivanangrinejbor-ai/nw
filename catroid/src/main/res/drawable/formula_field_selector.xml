<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Состояние: когда элемент нажат -->
    <item android:state_pressed="true"
        android:drawable="@drawable/formula_field_background_pressed" />

    <!-- Состояние по умолчанию (когда ни одно из верхних не подходит) -->
    <item android:drawable="@drawable/formula_field_background_normal" />
</selector>